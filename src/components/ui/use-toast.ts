/**
 * Re-exports the toast hook and function from the hooks directory
 * This file serves as a convenient entry point for toast functionality
 * 
 * @see @/hooks/use-toast for implementation details
 */

import { useToast, toast } from "@/hooks/use-toast";

export { useToast, toast };
