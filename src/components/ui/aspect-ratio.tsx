import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

/**
 * AspectRatio component for maintaining a consistent aspect ratio for its children
 * Built on top of Radix UI's AspectRatio primitive
 * Features:
 * - Ensures content scales responsively while preserving aspect ratio
 * - Useful for images, videos, and other media
 */
const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
